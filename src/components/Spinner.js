import React from 'react';
import {Oval} from "react-loader-spinner"
const Spinner=()=> {
  return (
    <Oval color="#FFFFFF" secondaryColor='#000000' strokeWidth={4} height={30} width={30} />
  );
}

export default Spinner;
